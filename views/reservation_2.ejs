<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title>공알이</title>
    <link rel="stylesheet" href="/stylesheets/reservation.css">
    <script>
    window.onload = function () {
            var temp = "<p>공공 시설명: " + data.Fname + "</p>";
            temp += "<p>선택한 날짜: " + sessionStorage.getItem('selectedDate'); + "</p>"; 
            temp += "<p>시간 선택</p>"
            document.getElementById("info_container").innerHTML = temp;
            setTimerange('주말');
            //sessionStorage.getItem('selectedDate').weektype
        };
    </script>
</head>

<body style="background:#1b182e; color:white; text-align:center;">
    <p style="margin:20px 20px 10px 20px;; font-size:30px;">공공시설 대관 예약_2</p>
    <div id='info_container'></div>
    <div style="margin-top:5px;">
        <select id="starttime">
            <option>시작시간 선택</option>
        </select>
        <select id="endtime">
            <option>종료시간 선택</option>
        </select>
    </div>
    <div style="margin-top:15px;">
        <input type="button" class='btn_choose' value='취소하기' onclick="cancel();">
        <input type="button" class='btn_choose' value='예약하기' onclick="goReservStep3();" >
    </div>
</body>
<script src='/javascripts/make_reserv.js'>
</script>

</html>